Khaseemvali Portfolio Website
=============================

Files included:
- index.html
- css/style.css
- js/script.js
- images/profile.svg
- README.txt

How to run:
1. Unzip the archive (if zipped).
2. Open `index.html` in any modern web browser (Chrome, Firefox, Edge).
   - No server is required for this static site.
   - For full features, simply open the file or host on GitHub Pages / Netlify if desired.

Customization:
- Edit `index.html` to update text content.
- Replace `images/profile.svg` with your own photo (keep the same path) to show a real image.
- CSS is in `css/style.css` — change colors, spacing, animations there.
